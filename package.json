{
  "name": "core",
  "version": "0.3.3",
  "description": "Shared config, logging, and app-detection foundation for the intisy-ai AI-proxy ecosystem.",
  "type": "module",
  "license": "MIT",
  "author": "intisy",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/intisy-ai/core.git"
  },
  "keywords": [
    "opencode",
    "claude",
    "core",
    "plugin",
    "commands",
    "config"
  ],
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/",
    "teavm-build.mjs",
    "README.md",
    "LICENSE"
  ],
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "emit": "node api/scripts/emit-dts.mjs --java-dir java --module :contracts --module-dir contracts --out src/generated",
    "build": "npm run build:deps && tsc --emitDeclarationOnly && node build.mjs",
    "build:deps": "npm --prefix api install && npm --prefix api run build",
    "test": "npm run build:deps && vitest run",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@intisy-ai/api": "file:api"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.5.0",
    "vitest": "^3.0.0"
  }
}
