package io.github.intisy.ai.contracts.ui;

import io.github.intisy.ai.tsemit.TsEnum;

/** Element type of a list field. */
@TsEnum
public enum ItemType {
    /** Each element is free text. */
    string,
    /** Each element is numeric. */
    number
}
